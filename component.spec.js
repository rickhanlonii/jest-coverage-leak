import Component from './component.js';

describe('Component', () => {
  it('should render with no props', () => {
    // Notice that the component isn't even rendered
    expect(true).toBeTruthy();
  });
});
